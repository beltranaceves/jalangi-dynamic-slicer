import type { ESTree } from "meriyah";
export interface WalkerContext {
    skip: () => void;
    remove: () => void;
    replace: (node: ESTree.Node) => void;
}
export declare class WalkerBase {
    should_skip: boolean;
    should_remove: boolean;
    replacement: ESTree.Node | null;
    context: WalkerContext;
    constructor();
    replace(parent: any, prop: string, index: number | null | undefined, node: ESTree.Node): void;
    remove(parent: any, prop: string, index: number | null | undefined): void;
}
